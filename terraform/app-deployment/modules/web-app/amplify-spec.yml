version: 1
frontend:
  phases:
    build:
      commands: 
        - echo "No build required for static site"

  artifacts:
    baseDirectory: /frontend
    files:
      - '**/*' # All files
  
  cache:
    paths: []